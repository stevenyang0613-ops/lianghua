/**
 * Re-export canonical types from shared/ — DO NOT add interface members here.
 * Edit shared/types/electron.d.ts instead.
 */
export type { ElectronAPI, UpdateStatus, CrashReport } from '@shared/types/electron'
